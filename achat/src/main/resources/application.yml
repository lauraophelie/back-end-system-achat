spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/systeme_commercial
    username: commerce
    password: commerce
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate: 
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
